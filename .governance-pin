# WHICH GOVERNANCE COMMIT THIS COMMIT WAS VERIFIED AGAINST.
#
# governance/ (schema, sql, sp, tests) lives in a separate PRIVATE repository
# and is no longer a submodule -- it used to appear as a gitlink entry in this
# repository's file listing, which read as though the private material were
# published here. It never was: a gitlink is a commit id, and every clone of
# that path 404s without access.
#
# But the gitlink was doing one real job besides fetching, and this file keeps
# it: it recorded WHICH governance commit a given public commit was verified
# against. Without a pin, CI would verify each commit against whatever
# governance/main happened to be at the time, and a green tick would no longer
# identify what was actually checked. That is the same class of defect as a
# test that passes for the wrong reason.
#
# Update this whenever governance changes, in the same commit that depends on
# the change -- exactly as the gitlink had to be updated.
206c9073f60c00aec463fca46e17c0bae94dbec1

# e802469 -- the governance chain PROMOTED into REQUIRED_MIGRATIONS, and the
# baseline regenerated to carry the resulting ledger. The pin matters more
# than usual here: deploy_state now DECLARES five files as part of the
# governed chain, and migrate --check reads that as a claim about what has
# run. A public commit pinned to a governance commit without them would
# fail with 'DECLARED BUT ABSENT FROM DISK'.

# dadcc53 -- the P0/P1 remediation. TWO new SQL files arrive with this pin and
# NEITHER is in REQUIRED_MIGRATIONS: governance_decision_link_identity.sql and
# workflow_owner_resolution.sql are both classified out_of_band, PENDING
# DEPLOYMENT. That is the opposite of the e802469 hazard above and needs the
# same care for the mirror-image reason. There, a public commit pinned too
# EARLY would fail 'DECLARED BUT ABSENT FROM DISK'. Here, promoting either file
# before Railway has executed it would make migrate --check assert a chain
# production has not run -- so the promotion belongs in the same change that
# records the Railway application, and not in this one.
#
# Also carries the test changes those files are verified by, including
# test_policy_governance.py, which is new and gated.

# f84400c -- the two P0/P1 files PROMOTED into REQUIRED_MIGRATIONS after Railway
# executed them, and the baseline regenerated to carry the resulting ledger.
# This pin is the e802469 hazard again and in the same direction: deploy_state
# now declares 48 files, and a public commit pinned to a governance commit
# without them would fail with 'DECLARED BUT ABSENT FROM DISK'.

# 8efd4be -- the baseline splice fix. CI could not build the database from
# f84400c: two concatenated dumps left an unpaired \unrestrict directive.
# Proven by scripts/verify_gate against a scratch database this time.

# feded6c -- the a2a read/write split. The fixture had to mount both routers.

# f32c691 -- the Defect A/B remediation set, and the pin matters in the
# e802469 direction again: deploy_state now declares THREE more files as
# out_of_band, so a public commit pinned earlier than this would fail with
# 'DECLARED BUT ABSENT FROM DISK'.
#
# Two of the three have been applied to Railway and are deliberately NOT
# promoted into REQUIRED_MIGRATIONS -- that list is a claim about what
# production has RUN, and promotion belongs in the change that records the
# read-back, not in the change that applies the file:
#     staff_email_ledger_governance_kinds.sql       applied, not promoted
#     escalation_assigned_requires_claim_time.sql   applied NOT VALID, not promoted
#     escalation_reminder_allocation.sql             NOT applied anywhere
#
# The third is dormant capability: no producer, no scheduler, no REMIND_*
# configuration, no ledger integration, and zero reminder_* columns on Railway.
# Its SQL and its design proofs are committed so the reasoning is auditable; a
# committed migration is not an activated one.
#
# Also carries: the ledger identity boundary's tests, the escalation claim
# concurrency proofs, the cap-ownership fixture correction, and the fixture
# leak that opened the global send breaker and failed twelve unrelated tests.

# 48332dc -- the alert ack-window and resolution-disposition change (A-02,
# A-06). The e802469 hazard once more: deploy_state declares ONE more
# out_of_band file, governance_alert_ack_and_disposition.sql, so a public
# commit pinned earlier than this would fail with 'DECLARED BUT ABSENT FROM
# DISK'.
#
# NOT promoted into REQUIRED_MIGRATIONS, and it must not be until Railway has
# run it. It is also the first pin in this file whose DEPLOY ORDER is
# load-bearing across two deployment paths: governance-mgmt.html has to ship
# BEFORE the app and the SQL, because the console is what sends the new
# disposition field and an older console against the new rule makes every
# Resolve button fail. The console is hand-deployed and untracked, so nothing
# in CI can enforce that ordering -- it is written here because this is the
# file the next person reads when they wonder why the pin moved.
#
# Also carries the three test files it is verified by: one new
# (test_alert_ack_and_disposition.py, 15 cases) and two updated, where the
# updates supply the new field rather than relax what the tests measure.

# 0973596 -- the same change split into two SQL files. deploy_state now
# declares TWO more out_of_band files rather than one, so the e802469 hazard
# applies to this pin and not to 48332dc: a public commit pinned to 48332dc
# would fail 'DECLARED BUT ABSENT FROM DISK' on
# governance_alert_resolution_required.sql.
#
# The split exists because the deploy order could not be made safe otherwise.
# With the requirement in the same file as the columns, schema-first makes the
# trigger demand a field the running app does not write and app-first makes the
# app write a column that does not exist -- both break Resolve. Neither file is
# promoted, and the LAST of them must not be applied until the app is live.

# 5284513 -- readonly_role.sql (D-08). The e802469 direction again: deploy_state
# declares one more out_of_band file, so a public commit pinned earlier than
# this would fail 'DECLARED BUT ABSENT FROM DISK'.
#
# Not promoted, and promotion is NOT what closes D-08 -- rotating the postgres
# password is, and no file in this repository can do that.

# 18e7e4b -- the A-05 notification-delivery test suite. Tests only; no SQL file
# arrives with this pin, so neither the e802469 hazard (declared-but-absent) nor
# its mirror applies. It is a DESCENDANT of 5284513, so this commit also carries
# readonly_role.sql and depends on the deploy_state entry that declares it --
# which is why this branch is stacked on feat/readonly-role-d08 rather than cut
# from master. Merging it before that one would fail the disposition test with a
# file on disk and no classification.

# fdce6f3 -- a comments-only correction to readonly_role.sql's runbook. No SQL
# statement changed and no database needs re-applying it; the pin moves only so
# a checkout of this commit gets the corrected instructions rather than the ones
# that would have produced a Railway password desync.

# 80a7b64 -- the A-15 declaration tests. Tests only, no SQL, so neither the
# e802469 hazard nor its mirror applies. They pin an ABSENCE -- that alert
# discharge does not consult accountable_owner -- which is the kind of property
# a later reader removes by mistake, believing they are fixing an oversight.

# 206c907 -- the D-01 retirement tests. Tests only; no SQL, no application code.
# They assert that auth_dep.require_data_access refuses anonymous customer-subject
# reads under the posture `locked` sets, and -- the half that makes the first half
# mean something -- that the same reads are ALLOWED under public-read, so the
# suite cannot pass on a gate that refuses everything or on a machine already
# configured locked.

