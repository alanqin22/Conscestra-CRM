fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-ollama>=0.1.0
# U5 provider failover — alternate provider client. Only imported when
# LLM_FAILOVER_ENABLED=1 and the primary actually fails.
langchain-google-genai>=2.0.0
langchain-community>=0.2.0
langgraph>=0.1.0
httpx>=0.27.0
apscheduler>=3.10,<4
ddgs>=9.0.0
trafilatura>=1.12.0
# PINNED BELOW 2.0. mcp 2.0.0 removed `mcp.server.fastmcp` (the class moved
# under `mcp.server.mcpserver`), so `mcp>=1.0` silently resolved to a release
# that cannot import app/mcp_server.py. CI had been red since 2026-08-01 for
# this reason alone — an unpinned major range is a scheduled outage whose date
# is chosen by someone else.
# Migrating to the 2.x API is real work and needs testing against a live MCP
# client; that is a separate change, not a dependency bump.
mcp>=1.0,<2
PyMuPDF>=1.24          # kb_ingest: PDF text extraction (import fitz)
python-multipart>=0.0.9  # kb_ingest: FastAPI file-upload route
audioop-lts>=0.2; python_version >= "3.13"  # voice_stream: audioop left stdlib in 3.13
numpy>=1.26          # vectorized cosine ranking in app/core/embeddings.py
