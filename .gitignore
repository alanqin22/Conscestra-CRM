# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# Deployment
deploy_config.ps1
deploy_winscp.log
# HTML pages deploy separately to agentorc.ca (cPanel) — never commit them.
*.html

# Scratch/working files and Railway SQL deploys — local only, never commit.
/scratch/
/sql/
/sp/
*.sql

# Tests — local only, by request. They stay in the working tree and still run
# with `python -m pytest tests/`; they are simply not published.
#
# CONSEQUENCE, recorded so it is not rediscovered: CI can no longer run them.
# The pipeline is reduced to importing every app module, which still catches
# syntax errors, broken imports and circular dependencies across the codebase —
# but NOTHING else is verified on GitHub. All 1039 tests, and every
# database-layer control, are now verified only on this machine and on Railway
# via `python -m scripts.verify_invariants`.
/tests/

# Claude Code workspace (skills, local settings) — local only.
.claude/

# ── Loose credential files (never commit) ──────────────────────────────
authentication.txt
docs/raillway password and url.txt
docs/railway upload.txt
45b23d9a1f8e4c7b8d2a6c9e3f1b4d8a.txt
backups/
